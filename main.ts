/**
 * Entry point: re-exports the App component from the structured source.
 * Use this file when the bundler expects main.ts as the root (e.g. StackBlitz, Replit).
 */
export { default } from './src/App';
